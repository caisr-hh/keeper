# KEEPER landing page

A small, static landing page for the KEEPER research project.

Open `index.html` directly, or serve the directory with any static file server.
There are no dependencies or build steps.
